/*
 * Escribir un programa que calcule la cantidad de dinero que uno tendría al
 * cabo de t años invirtiendo P pesos con una tasa de interés anual r.
 * La fórmula que hay que aplicar es Pe^(rt).
 * Usar la función `exp()` de `math.h` para calcular e^x.
 */
